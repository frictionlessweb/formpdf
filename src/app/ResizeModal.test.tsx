import React from "react";
import ResizeModal from "./ResizeModal";
import { render } from "../testUtils";

describe("Our ResizeModal component", () => {
  test("Renders properly", () => {
    render(<ResizeModal />);
  });
});
